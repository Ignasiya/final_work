package core.data;

import java.util.List;

public class Cat extends Pet {
    public Cat(int id, String name, String birthday, List<String> commands) {
        super(id, name, birthday, commands);
    }
}
